class foremancorepackages {
package { 'telnet':
        ensure => latest,
      } 
package { 'php':
        ensure => latest,
      }
package { 'php-mssql':
        ensure => latest,
      }
package { 'rrdtool':
        ensure => latest,
      }
package { 'rrdtool-devel':
        ensure => latest,
      }
package { 'perl-RRD-Simple':
        ensure => latest,
      }
package { 'net-snmp':
        ensure => latest,
      }
package { 'net-snmp-utils':
        ensure => latest,
      }
package { 'ntp':
        ensure => latest,
      }
package { 'mysql':
        ensure => latest,
      }
package { 'perl-CPAN':
        ensure => latest,
      }
package { 'perl-Net-SNMP':
        ensure => latest,
      }
package { 'nagios-plugins-icmp':
        ensure => latest,
      }
package { 'perl-Config-IniFiles':
        ensure => latest,
      }
package { 'ant':
        ensure => latest,
      }
package { 'ant-nodeps':
        ensure => latest,
      }
package { 'lzo-devel':
        ensure => latest,
      }
package { 'gnuplot':
        ensure => latest,
      }
package { 'rubygems':
        ensure => latest,
      }
package { 'icinga':
        ensure => latest,
      }
package { 'icinga-idoutils-libdbi-mysql':
        ensure => latest,
      }
}
